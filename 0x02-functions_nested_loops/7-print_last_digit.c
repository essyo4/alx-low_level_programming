#include "main.h"

/**
 * print_last_digit - prints last digit
 * @n: the number
 * Return: value of last digit
 */
int print_last_digit(int n)
{
	int x;

	if (n < 0)
		n = -n;
	_putchar(x + '0');
	return (x);
}
