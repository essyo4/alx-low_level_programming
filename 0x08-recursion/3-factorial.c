#include "main.h"

/**
 * factorial - return factorial of number
 * @n: number
 * Return: if n > 0 return factorial else return 1
 */

int factorial(int n)
{
	int f = n;

	if (n < 0)
		return (-1);

	else if (n >= 0 && n <= 1)
		return (1);
}
