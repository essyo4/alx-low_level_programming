#include "main.h"

/**
 * print_line - prints straight line
 * @n: number of time character is printed
 */

void print_line(int n)
{
	int l;

	if (n > 0)
	{
		for (l > 0; l < n; l++)
			_putchar('_');
	}
	else
	{
		_putchar('\n');
	}
}
